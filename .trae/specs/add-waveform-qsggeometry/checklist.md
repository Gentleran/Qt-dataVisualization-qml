- [ ] ChartPannel.qml 包含上下两个等高的图表区域，各有标题标签
- [ ] WaveformItem 类使用 QML_ELEMENT 宏声明，能编译通过，继承 QQuickItem 并实现 updatePaintNode()
- [ ] qt_add_qml_module 配置正确，QML 中 import WaveformItem 后可使用 WaveformItem 组件
- [ ] main.cpp 中无手动 qmlRegisterType<WaveformItem>() 调用
- [ ] ChartPannel 中 WaveformItem 显示静态测试波形
- [ ] WaveformItem 的坐标轴属性（xMin/xMax/yMin/yMax/xTickCount/yTickCount/axisMargins）可从 QML 设置
- [ ] updatePaintNode() 使用场景图节点树（root → axisNode + waveNode），坐标轴和波形独立更新
- [ ] 坐标轴绘制包含：边框线、Y轴刻度线、X轴刻度线、网格线
- [ ] 波形数据映射到 yMin/yMax 范围内显示，超出范围的数据被裁剪
- [ ] 波形绘制在绘图区域内（考虑 axisMargin 边距）
- [ ] geometryChange() 被正确重写，窗口缩放时波形和坐标轴自动适应新区域
- [ ] 窗口放大后波形和坐标轴扩展到新区域
- [ ] 窗口缩小后波形和坐标轴收缩，不超出可视区域
- [ ] Y 轴左侧显示刻度数值标签，与刻度线位置对齐
- [ ] X 轴底部显示刻度数值标签，与刻度线位置对齐
- [ ] 刻度标签根据 yMin/yMax/xMin/xMax 和 tickCount 计算正确的数值
- [ ] WaveformItem 的 lineColor 属性可从 QML 设置，折线颜色可自定义
- [ ] WaveformItem 的 lineWidth 属性可从 QML 设置
- [ ] WaveformDataSource 类使用 QML_ELEMENT + QML_SINGLETON 声明，能编译通过
- [ ] WaveformDataSource 包含环形缓冲区和 Min-Max 降采样方法
- [ ] 模拟数据 Timer 能驱动 WaveformItem 动态更新
- [ ] TcpClient 的 onReadyRead() 能将二进制数据解析为 float 数组并传递给数据源
- [ ] TCP 连接后，时域波形图实时显示接收到的数据
